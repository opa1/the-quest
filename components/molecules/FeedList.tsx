"use client"

// FeedList is superseded by RecentMissions + RealmActivityLog on the realm page.
// Kept as a placeholder to avoid broken imports elsewhere.
export default function FeedList() {
  return null
}
